{{- /* Set safe defaults for all service account parameters */ -}}
{{- $serviceAccountValues := .Values.serviceAccount | default dict -}}
{{- $globalServiceAccount := .Values.global.serviceAccount | default dict -}}
{{- $enabled := (($serviceAccountValues.enabled | default true) and ($globalServiceAccount.enabled | default true)) -}}
{{- $create := (($serviceAccountValues.create | default true) and ($globalServiceAccount.create | default true)) -}}

{{- if and $enabled $create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "gitlab.serviceAccount.name" . }}
  namespace: {{ .Release.Namespace | default "gitlab" }}
  labels:
    {{- include "gitlab.standardLabels" . | nindent 4 }}
    {{- include "gitlab.commonLabels" . | nindent 4 }}
    {{- with $serviceAccountValues.labels -}}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- with $globalServiceAccount.labels -}}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- $annotations := merge ($serviceAccountValues.annotations | default dict) ($globalServiceAccount.annotations | default dict) -}}
  {{- if $annotations }}
  annotations:
    {{- toYaml $annotations | nindent 4 }}
  {{- end }}
{{- if $serviceAccountValues.automountServiceAccountToken | or $globalServiceAccount.automountServiceAccountToken }}
automountServiceAccountToken: {{ $serviceAccountValues.automountServiceAccountToken | default $globalServiceAccount.automountServiceAccountToken | default false }}
{{- end }}
{{- end }}
