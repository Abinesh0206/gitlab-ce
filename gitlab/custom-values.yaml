global:
  edition: ce

  hosts:
    domain: gitlab.example.com   # Change to your domain
    externalIP: ""               # optional

  ingress:
    configureCertmanager: false
    tls:
      enabled: false

  psql:
    host: "dev-db.cyxw2mqqud77.us-east-1.rds.amazonaws.com"
    port: 5432
    database: "mydatabase"
    username: "dbadmin"
    password:
      secret: "gitlab-postgres-secret"
      key: "password"
    ssl:
      enabled: false  # Or true if RDS enforces SSL
      secret: ""
certmanager:
  install: false

nginx-ingress:
  enabled: false

postgresql:
  install: false

redis:
  install: true

minio:
  enabled: false

gitlab-runner:
  install: false
