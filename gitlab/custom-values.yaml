global:
  edition: ce

  hosts:
    domain: gitlab.example.com   # Change to your real domain
    externalIP: ""               # Optional, leave blank if not needed

  ingress:
    configureCertmanager: false
    tls:
      enabled: false

  psql:
    host: "dev-db.cyxw2mqqud77.us-east-1.rds.amazonaws.com"
    port: 5432
    database: "mydatabase"
    username: "dbadmin"
    password:
      secret: "gitlab-postgres-secret"  # Create this secret with your DB password
      key: "password"
    ssl:
      enabled: false
      secret: "gitlab-ssl-dummy"        # Create this dummy secret as explained earlier
      serverCA: "dummy-server-ca.pem"
      clientKey: "dummy-client.key"
      clientCertificate: "dummy-client.crt"

certmanager:
  install: false

nginx-ingress:
  enabled: false

postgresql:
  install: false

redis:
  install: true

minio:
  enabled: false

gitlab-runner:
  install: false

gitlab:
  rails:
    artifacts:
      objectStore:
        enabled: false
    lfs:
      objectStore:
        enabled: false
    uploads:
      objectStore:
        enabled: false
    packages:
      objectStore:
        enabled: false
    backups:
      objectStore:
        enabled: false

toolbox:
  backups:
    objectStorage:
      enabled: false
